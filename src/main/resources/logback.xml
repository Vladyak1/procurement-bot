<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/app/logs/procurement-bot.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>/app/logs/procurement-bot.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
            <!-- Жёсткий потолок на все логи разом. 26.08.2026 диск VPS забился под 100%,
                 из-за чего бот молча перестал писать и в логи, и в БД (SQLite там же),
                 но продолжал публиковать — лоты уходили в канал по кругу. -->
            <totalSizeCap>300MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- Quartz на DEBUG писал «batch acquisition of 0 triggers» каждые ~25 секунд —
         это и было основным объёмом логов. -->
    <logger name="org.quartz" level="WARN"/>
    <logger name="org.telegram" level="INFO"/>
    <logger name="org.apache.http" level="WARN"/>

    <!-- INFO вместо DEBUG: DEBUG дублировался в файл И в консоль (docker json-log),
         то есть каждая строка ложилась на диск дважды. -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>